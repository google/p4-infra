# PDPI Documentation Assets

This directory contains pre-rendered Scalable Vector Graphics (`.svg`) and their
corresponding Graphviz (`.dot`) source files supporting the PDPI documentation.

To regenerate an SVG from its DOT source, run:

```bash
dot -Tsvg filename.dot > filename.svg
```
